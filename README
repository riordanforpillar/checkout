Checkout kata from
https://github.com/PillarTechnology/kata-checkout-order-total

This code is written in python3 and it is assumed that python3 is installed
and in the PATH environment variable for execution


Linux (and presumably OS X, not tested):

To run all tests:

./runAllTests.sh

To run a simple example:

./runExample.sh


Windows from the command line

To run all tests:

runAllTests.bat

To run a simple example:

runExample.bat
